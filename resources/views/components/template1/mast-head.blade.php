<header class="masthead">
    <div class="container">
        <div class="masthead-subheading">{{$content['subheading']}}</div>
        <div class="masthead-heading text-uppercase">{{$content['heading']}}</div>
        <a class="btn btn-primary btn-xl text-uppercase" href="#services">{{$content['buttonText']}}</a>
    </div>
</header>